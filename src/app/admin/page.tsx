import AddArtForm from "./AddArtForm";
const page = async () => {
  return (
    <section className="grid h-[80vh] place-items-center">
      <AddArtForm />
    </section>
  );
};

export default page;
